const elem = document.getElementById("YOLO");


function ASD() {
    console.log();  // Muestra el botón que ha disparado el evento
    elem.style.color = "red";   // Cambia el color del elemento YOLO
    }